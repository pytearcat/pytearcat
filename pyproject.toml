[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"


[tool.pytest.ini_options]
testpaths = ['tests']
filterwarnings = [
  "ignore:::jupyter_client.connect:22", # jupyter client deprecation warning when testing
]


